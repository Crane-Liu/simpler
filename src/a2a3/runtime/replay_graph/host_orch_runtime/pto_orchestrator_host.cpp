// Compile the shared orchestrator implementation into the host runtime without
// adding the whole runtime directory, which also contains the device scheduler.
#include "../runtime/pto_orchestrator.cpp"
